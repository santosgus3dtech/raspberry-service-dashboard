FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY raspberry_dashboard ./raspberry_dashboard

EXPOSE 8080
CMD ["uvicorn", "raspberry_dashboard.app:app", "--host", "0.0.0.0", "--port", "8080"]
